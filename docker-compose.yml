services:
  python:
    build: .
    container_name: ia_python_biblioteca
    volumes:
      - ./src:/app/src
      - ./notebooks:/app/notebooks
      - ./data:/app/data
      - ./outputs:/app/outputs
    working_dir: /app
    command: tail -f /dev/null